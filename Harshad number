def SumOfDigits(num):
    sum = 0
    for i in str(num):
        sum = sum + int(i)
    return sum



def HarshadNumber(num):
    add = SumOfDigits(num)
    if num%add==0:
        return "Harshad"
    else:
        return "Not Harshad"
