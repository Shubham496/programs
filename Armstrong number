def Armstrong(num):
    power = len(str(num))
    add = 0
    for n in str(num):
        add = add + int(n)**power
    if add == num:
        return 'Armstrong'
    else:
        return "not Armstrong"
